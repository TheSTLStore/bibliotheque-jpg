@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-background text-text-primary antialiased;
  }
}

@layer components {
  .card {
    @apply bg-background-card rounded-xl border border-border p-4 transition-all hover:border-border-light;
  }

  .btn-primary {
    @apply bg-accent text-background font-semibold px-4 py-2 rounded-lg hover:bg-accent-hover transition-colors;
  }

  .btn-secondary {
    @apply bg-background-light text-text-primary border border-border px-4 py-2 rounded-lg hover:border-border-light transition-colors;
  }

  .input-field {
    @apply bg-background-light text-text-primary border border-border rounded-lg px-3 py-2 focus:outline-none focus:border-accent placeholder:text-text-muted;
  }
}
